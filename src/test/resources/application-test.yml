spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: password

  redis:
    host: localhost
    port: 6379
    timeout: 2000ms

app:
  xdb:
    cache:
      enabled: true
      ttl-seconds: 300
  notification:
    email:
      from: hex4w@localhost
      endpoint-enabled: true
  graphql:
    enabled: true

logging:
  level:
    root: WARN
    co.onmind.hex4w: INFO
    org.springframework.r2dbc: INFO